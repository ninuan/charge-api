# 原型验证记录

验证日期：2026-09-11。测试对象：`/prototype/`，不是旧页面或静态截图。

## 自动化

- 全量前端测试：47 个文件、162 项通过，其中新增原型测试 28 项。
- 原型 ESLint：零错误、零警告。
- TypeScript 与 Next.js 生产构建通过，`/prototype` 可静态预渲染。
- `git diff --check` 通过。既有正式页面修改未被覆盖。

复测：

```sh
pnpm --dir frontend exec vitest run app/prototype
pnpm --dir frontend test
pnpm --dir frontend exec eslint app/prototype --max-warnings 0
pnpm --dir frontend exec tsc --noEmit --incremental false
pnpm --dir frontend build
```

## 实际浏览器检查

通过本机运行页面执行点击、输入、键盘操作，并检查 DOM、布局尺寸及控制台，不以截图代替操作验证。

| 场景 | 检查结果 |
| --- | --- |
| 1440 / 1024 / 768 / 390 / 320 像素常用桩详情 | 无页面横向溢出；窄屏使用列表 / 详情分步 |
| 320 像素全部个人及管理员页面 | 无页面横向溢出；管理表格在自己的容器内滚动 |
| 桩列表 → 提醒管理 → 保存 4 小时 | 截止时间更新；弹窗关闭后焦点回到原按钮 |
| 快速搜索「南门」 | 正确定位南门桩；历史可切 24 小时 / 7 天 / 30 天及单口 |
| 模拟发现空闲 → 通知 → 具体端口 | 提醒自动结束，生成消息，返回对应桩与端口详情 |
| 新桩添加失败 → 保留输入 → 重试 | 首次失败不改数据；重试成功；未读取状态不冒充离线或可用 |
| 新桩 → 模拟首次读取 | 从未知状态进入明确标注的示例端口状态 |
| 离线快照 | 禁止刷新和保存；去掉「可以使用」的当前可用表述；状态列不挤压 |
| 深色 + 始终减少动态效果 | 深色变量生效；加载动画计算样式为 `none` |
| 管理异常 → 处理记录 → 受影响用户诊断 | 用户和异常互相定位；处理记录保存 |
| 系统策略修改 | 调度说明同步更新，并写入操作记录 |
| 失效账户手动刷新 | 显示错误反馈，审计结果为失败，不误报成功 |
| 微信测试发送 → 查询结果 | 查询不重复发送；只表示渠道已提交，不表示手机送达或已读 |
| 扫码模拟与登录 / 注册页面 | 明确标注模拟，不发送真实密码、Cookie 或微信授权 |

已修正浏览器检查发现的辅助文字过小、浅色状态文字对比度、窄屏旧状态列挤压、管理表格隐藏辅助文字造成的页面溢出，以及加载按钮宽度跳动等问题。

## 边界

- 示例数据使用单独的浏览器存储键 `charge.prototype.v2`，不调用业务 API。
- 搜索、切换、表单、状态变化和管理记录可操作；真实远端数据接入、真实认证、二维码授权、通知投递由现有后端承担，不在本轮原型中执行。
- 没有部署、创建真实账户、触发真实通知或改动现场设备。
