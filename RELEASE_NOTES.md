# Charge Console v1.5.2 发布说明

v1.5.2 把空闲提醒改为“需要时开启”的临时任务，并在保留站内通知的基础上增加 WxPusher 微信提醒。第三方服务异常不会中断充电桩刷新、状态历史或站内通知。

## 用户可见变化

- 空闲提醒默认持续 2 小时，也可选择 1 小时、4 小时或到计划断电前；首次发现空闲、到期或手动取消后停止查询。
- 固定星期和时段提醒移动到高级设置，适合确实需要长期提醒的用户。
- 通知中心支持微信扫码绑定、消息类型选择、临时关闭、测试消息、最近投递结果和解除绑定。
- 消息状态明确区分已加入队列、WxPusher 已受理和 WxPusher 已处理；这些状态不承诺某台微信客户端已经展示或已读。
- 免打扰时段只保留站内通知，不弹出浏览器通知或发送微信消息。

## 部署前准备

1. 备份 `/var/lib/charge-api/charge_state.db`，并执行 `PRAGMA integrity_check;`。
2. 在 WxPusher 创建测试或正式应用，取得 AppToken；不要把真实值写入仓库、前端变量或截图。
3. 在 `charge-api` 的服务端环境文件增加：

   ```text
   WXPUSHER_APP_TOKEN=AT_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   PUBLIC_BASE_URL=https://charge.example.com
   ```

4. `PUBLIC_BASE_URL` 只能包含 HTTPS 协议和域名。生产环境保持官方 WxPusher API 默认值，不配置 `WXPUSHER_BASE_URL`。

未配置 AppToken 也可以安全升级：微信入口会显示暂未开放，站内通知和浏览器提醒不受影响。

## 升级与验证

部署后依次检查：

1. `curl -fsS http://127.0.0.1:8080/healthz` 返回版本 `1.5.2`。
2. 管理员“运维审计”可以看到临时提醒、固定提醒、24 小时桩号请求数和 WxPusher 投递状态。
3. 普通用户在通知中心获取二维码，扫码后页面自动显示绑定成功。
4. 开启所需消息类型并发送测试消息，确认页面状态和实际接收端符合预期。
5. 创建一个 1 小时临时提醒：如果当前无空闲口，应显示下次检查和预计请求次数；真实出现空闲后只通知一次并自动完成。
6. 在 375px 宽度下复查临时提醒、固定提醒、微信设置和解绑操作。

标准推送 API 本身没有微信 ClawBot 的 24 小时 10 条限制。只有选择 ClawBot 作为接收渠道时才需要按 WxPusher 页面提示激活；WxPusher 手机端和桌面端不受该限制。

## 数据与可靠性

- 启动时自动把 SQLite Schema v9 升级到 v10，原有提醒迁移为固定时段提醒。
- WxPusher UID 和扫码会话代码使用现有 `CHARGE_COOKIE_KEY` 加密保存；AppToken 只存在于进程环境。
- 外部投递使用 SQLite 持久队列。服务重启后继续未完成任务，已经受理的消息不会重新发送。
- 可重试错误最多尝试三次；免打扰期间抑制的消息不会在之后补发。

## 回滚

v1.5.2 会把数据库升级到 Schema v10。需要回滚时，先停止 `charge-api`，恢复部署前的数据库备份，再部署 v1.5.1 二进制；不要让 v1.5.1 直接打开已经升级的 v10 数据库。移除 `WXPUSHER_APP_TOKEN` 可以单独关闭微信能力，无需回滚应用。

## 发布验收门禁

- `make check` 全部通过。
- Playwright Chromium E2E 全部通过。
- 测试 WxPusher 应用完成一次扫码绑定、测试消息和真实临时空闲通知。
- Git 工作区不包含真实 AppToken、完整 UID、Cookie 或私有版本规划文档。
