# Charge Console v1.5.3 发布说明

本版完善添加前的微信绑定引导和扫码确认。未绑定用户点击“添加充电桩”即进入扫码步骤；微信授权后，“确认绑定”才可点击，保存成功后点击“继续添加充电桩”返回表单。

## 交互变化

- 添加前检查绑定能力与状态，不消耗设备刷新额度；已绑定用户直接填写，未配置扫码服务时可手动设置 Cookie。
- 添加失败如果明确需要绑定或重新扫码，可在同一弹窗中恢复。草稿保留，绑定完成不自动提交充电桩。
- 已扫码与已授权有明确区别；绑定保存成功、凭据同步成功也分别反馈。
- 连续三次检测失败暂停自动查询，支持手动重试；离线、过期、关闭和换码不会误启用确认按钮。
- 二维码归属由本站用户校验，确认请求可在同一会话内恢复结果，避免响应超时后盲目重复提交。

## 部署与回归

1. 按原有部署方式同时更新前后端，健康检查版本应为 `1.5.3`。无需数据库迁移或新增环境变量。
2. 现有 YYB 与 Mocele 配置均有效时启用扫码；能力字段不代表远端此刻健康。
3. 升级及后端重启后，未完成二维码重新生成；已保存的绑定和设备保留。
4. 二维码归属和确认结果短期保存在后端进程内（30 分钟）。多实例需要同一用户请求固定到同一实例；当前单实例部署无需调整。
5. 按 [验收记录](docs/plans/2026-09-13-v1.5.3-binding-acceptance.md) 在真实微信环境补测首次绑定、已有绑定重新扫码和同步失败反馈。自动化使用隔离接口，不替代真实扫码。

本版不新增后台保活，不承诺长期凭据永久有效，不改变刷新额度。
