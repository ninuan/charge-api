# Charge Console 安全加固计划

## 目标

降低公网部署中凭据泄露、远端响应导致的资源耗尽、部署命令注入和迁移残留数据暴露风险。完成后，所有上游响应都有明确大小上限，日志只保留经过脱敏和控制字符清理的诊断信息，部署参数按安全边界传递，迁移归档不再保存可用凭据。

## 问题与修复方案

### 1. 部署参数注入远程脚本（高风险）

**现状**

`SERVICE_NAME`、`HEALTH_URL` 从环境变量读取后直接插入 `deploy.sh` 和 `deploy_git.sh` 生成的远程 shell 脚本。特殊字符会改变远程命令结构。

**修复**

- 对 `SERVICE_NAME` 只接受 systemd 单元允许的字母、数字、`.`, `_`, `@`, `-`。
- 对 `HEALTH_URL` 做非空、协议和空白字符校验。
- 通过 shell 安全转义赋值到远程变量，并在远程命令中始终使用双引号展开。
- 为两个部署脚本增加恶意参数 dry-run 回归测试。

**验证**

- 恶意 `SERVICE_NAME`、`HEALTH_URL` 不会出现在可执行的额外命令位置。
- `scripts/deploy_test.sh` 和 `scripts/deploy_git_test.sh` 通过。

### 2. 上游响应体无大小上限（高风险）

**现状**

YYB、Mocèle 和充电桩客户端在多个位置直接 `io.ReadAll(resp.Body)`。恶意或异常上游响应会造成内存持续增长。

**修复**

- 增加统一的受限读取函数，读取上限为 1 MiB。
- 超出上限立即返回错误，并保留现有响应脱敏行为。
- 覆盖 YYB、Mocèle、充电桩桩号解析和充电桩状态请求。
- 增加超限响应测试。

**验证**

- 各客户端超限响应均返回受控错误，不保留完整响应体。
- Go 全量测试和 `go vet` 通过。

### 3. 远端响应内容进入日志（中风险）

**现状**

解析失败时会把远端响应片段放入错误；API 日志函数直接写入原始错误文本。响应中的凭据、换行符或控制字符可能进入日志系统。

**修复**

- 移除 `ParsePayload` 错误中的原始响应片段。
- 增加统一日志文本清理：先脱敏，再截断，并把换行符和控制字符转义为空格。
- API 的公开操作错误、结构化错误和 WxPusher 错误全部使用清理后的文本。
- 增加凭据和日志伪造字符测试。

**验证**

- 错误消息和日志中不出现 Cookie、Token、UID 等原文。
- 含换行符的上游响应不会生成伪造日志行。

### 4. 迁移归档保留密码哈希和邀请码（中风险）

**现状**

`*.migrated` 文件只清理 Cookie 和 YYB 绑定，仍包含用户密码哈希及可直接使用的邀请码，并长期留在服务器上。

**修复**

- 归档前清空 `Users[].PasswordHash` 和 `Invites[].Code`。
- 保留用户、邀请码的非敏感审计字段。
- 增加归档脱敏回归测试。

**验证**

- 归档文件不包含密码哈希、邀请码、Cookie、UID 或绑定凭据。
- 迁移后的 SQLite 数据仍可正常登录和读取业务状态。

### 5. 捕获请求允许明文 HTTP（中风险）

**现状**

捕获文件中的 URL 未校验协议，后续请求会带上 Cookie；配置为公网 HTTP 时会明文传输登录凭据。

**修复**

- 生产捕获请求只接受 HTTPS。
- 仅对 loopback 地址保留 HTTP，方便本地 mock 和测试。
- 增加 URL 校验测试，并保持重定向拦截。

**验证**

- 公网 HTTP、缺少主机或包含凭据的 URL 被拒绝。
- HTTPS 官方地址和 loopback HTTP 测试地址正常工作。

## 执行顺序

1. 先提交本计划文档。
2. 加固日志和响应读取，补充单元测试。
3. 修复迁移归档和捕获 URL 校验。
4. 修复两个部署脚本的参数传递与回归测试。
5. 运行 Go、前端和脚本检查。
6. 复查 Git diff、秘密扫描和工作区状态。

## 完成门槛

- `go test ./...` 通过。
- `go vet ./...` 通过。
- `pnpm test`、`pnpm lint`、`pnpm typecheck` 通过。
- 安全脚本和部署脚本测试通过。
- Git diff 中不出现真实 Token、Cookie、UID 或密码。
