# Charge Console 项目审查记录

审查日期：2026-09-05  
范围：Go 后端、Next.js 前端、部署与运维脚本、systemd 配置、依赖与测试配置。  
本次只记录问题与建议，不进行实现修改。

## 总体结论

当前项目已有较完整的认证、会话、CORS、请求限流、SQLite 加密存储、systemd 隔离和自动化检查。结合工作区中尚未提交的安全加固改动，已知的高风险项主要集中在部署脚本输入边界、上游响应处理和敏感数据生命周期；这些改动的验证结果已在既有安全计划中记录。

下面列出本轮复查仍建议评估的事项。优先级表示修复顺序建议：P0 立即处理，P1 尽快处理，P2 可排期改进，观察项先保留设计决定。

## 需要处理的问题

### P1：生产服务缺少显式请求体大小限制

**证据**：`backend/cmd/server/main.go` 设置了 `ReadHeaderTimeout`、`ReadTimeout` 和 `MaxHeaderBytes`，但没有设置 `http.Server.MaxBytesReader` 或路由级 body limit。部分 JSON handler 使用了自定义限制，仍需确认所有上传型或可扩展请求路径都覆盖。

**影响**：攻击者可以发送大请求体，增加内存和解析开销；如果未来新增 handler 忘记限制，会重新引入资源耗尽风险。

**建议**：在统一 middleware 中按路由类型设置 body 上限，例如普通 JSON 64 KiB、捕获或批量操作使用更高但固定的上限；为每个接受 body 的路由增加超限测试，并在审计脚本中检查关键接口。

**验证**：发送超过上限的 JSON、表单和 SSE 相关请求，确认返回 413 或明确错误，服务进程内存和日志保持稳定。

### P1：CSP 仍允许 `unsafe-inline`

**证据**：`backend/internal/api/middleware.go` 的 `contentSecurityPolicy` 包含 `script-src 'self' 'unsafe-inline'` 和 `style-src 'unsafe-inline'`。

**影响**：一旦出现 HTML 注入或第三方脚本拼接缺陷，脚本执行防线较弱。当前静态导出和 Next flight 数据是放行内联脚本的原因，属于架构约束而非已确认漏洞。

**建议**：评估 nonce 或 hash CSP 的可行性；至少收紧 `connect-src` 到实际使用的域名，并将 Cloudflare Insights 作为显式可选配置，未启用时不放行相关域名。

**验证**：在生产构建中检查所有脚本来源，使用浏览器 CSP 报告模式观察违规项，再切换到强制模式并运行 Playwright 测试。

### P1：初始管理员密码文件的生命周期依赖人工清理

**证据**：`backend/cmd/server/main.go` 将初始密码写入数据库目录下的 `initial-admin-password.txt`，权限为 `0600`，日志只记录文件路径；部署脚本排除了该文件同步，但服务启动后没有自动删除或设置一次性过期机制。

**影响**：服务器备份、运维误操作或长期遗留文件可能暴露初始凭据。密码文件与数据库目录同处时，备份策略也可能将其一并带走。

**建议**：首次成功修改管理员密码后自动删除文件；增加启动时文件存在时的告警和审计事件；备份脚本明确排除该文件，并补充文件生命周期测试。

**验证**：初始化、首次登录、修改密码、重启和备份场景下确认文件分别存在、删除、不会进入备份。

### P1：日志中的文件路径和错误链需要统一最小化

**证据**：`main.go`、调度器和部分管理处理器仍直接记录错误或本地绝对路径，例如数据库路径、模板路径、服务错误。

**影响**：绝对路径、内部服务名称和底层错误可能帮助攻击者进行环境指纹识别；错误链未来若包含上游响应，可能绕过当前已覆盖的日志点。

**建议**：统一日志封装，区分面向运维的受控字段和调试字段；路径只记录逻辑名称或脱敏后的 basename；对所有 error string 统一做脱敏、控制字符清理和长度上限。

**验证**：构造包含换行、Cookie、Token、绝对路径的错误，确认 journal 中没有敏感值、控制字符或多余路径信息。

### P2：第三方依赖缺少自动漏洞扫描门禁

**证据**：仓库已有 `go.mod`、`go.sum` 和 `frontend/pnpm-lock.yaml`，CI 主要执行测试、lint、构建和 Playwright；未发现 `govulncheck`、`npm audit`/审计服务或 Dependabot 配置。

**影响**：依赖出现公开漏洞时，CI 不一定会及时阻断或提示。

**建议**：在 GitHub Actions 增加 `govulncheck ./...`，前端接入锁文件审计或 Dependabot；设置允许失败的告警阶段，再根据误报情况转为合并门禁。

**验证**：在 CI 中确认 Go 和前端依赖扫描产出可追踪报告，并验证失败状态能阻止发布流程。

### P2：CORS 与反向代理信任边界需要部署级测试

**证据**：`WithCORS` 同时支持同源判断和 `CORS_ALLOWED_ORIGINS` 白名单；`clientIP` 逻辑依赖请求头和部署入口。应用自身没有 TLS，生产依赖 Nginx、Caddy 或 Cloudflare。

**影响**：反向代理错误配置可能让伪造的 `Origin`、客户端 IP 或转发头影响访问控制和限流效果。

**建议**：部署文档明确可信代理列表及转发头配置；生产验收加入真实代理链测试，确认外部请求无法直达后端、限流按真实客户端生效、任意 Origin 不会获得凭据 CORS。

**验证**：通过代理和直接访问两条路径分别测试 Origin、`Forwarded`、`X-Forwarded-For`、Cookie 和 OPTIONS 请求。

### P2：捕获请求目录的文件权限与符号链接边界需要固定

**证据**：`parser.ParseCaptureRequests` 遍历配置目录并读取 `basic`、`request_body`、`request_headers`；当前重点限制了 URL 协议，但没有看到目录权限、文件类型和符号链接检查。

**影响**：若生产配置目录被低权限用户写入，可能替换捕获文件读取任意本地内容，或把请求导向不应访问的 HTTPS 主机。

**建议**：启动时要求捕获目录及文件归属服务账号、权限不高于 0700/0600；拒绝符号链接和非普通文件；对 HTTPS 目标增加允许域名列表或配置项，并记录模板指纹。

**验证**：使用符号链接、FIFO、其他用户可写目录和非允许域名配置启动，确认服务拒绝加载并给出明确诊断。

### P2：通知和 SSE 的连接资源上限需要专项压测

**证据**：项目提供 SSE `/api/stream`、WxPusher 投递队列和后台调度器；HTTP server 有超时，但未见针对每个用户 SSE 连接数、全局连接数和写阻塞的明确上限。

**影响**：大量保持连接或慢客户端可能占用 goroutine、文件描述符和通知内存，影响普通请求。

**建议**：增加单用户与全局 SSE 连接上限、空闲清理和写超时；通知队列设置总量、单用户配额和监控指标；压测慢客户端与断网重连。

**验证**：模拟连接洪泛、慢读、频繁重连和 WxPusher 故障，确认健康检查、登录和刷新接口仍有稳定响应。

## 已确认的良好实践

- 默认监听回环地址，systemd 使用独立用户、`NoNewPrivileges`、`ProtectSystem` 和 `PrivateTmp`。
- Cookie、WxPusher UID 等敏感状态采用加密存储，AppToken 从服务端环境读取。
- CORS 不是通配符凭据模式，API 默认设置 `no-store`。
- 已有会话、登录失败限流、邀请和跨用户访问测试。
- 已加入上游响应大小限制、日志脱敏、迁移归档清理、捕获 URL 协议检查和部署参数注入回归测试。

## 建议处理顺序

1. 先补齐统一请求体限制、初始密码文件生命周期和日志最小化。
2. 评估 CSP nonce/hash 方案，并完成反向代理验收测试。
3. 增加捕获目录权限检查与 SSE/通知资源压测。
4. 将 Go 与前端依赖扫描加入 CI，再根据结果处理具体依赖升级。

## 本轮未发现的高风险问题

通过静态检查和现有测试未发现明显的匿名认证绕过、SQL 拼接注入、跨用户数据读取路径或将 WxPusher AppToken 暴露给前端的代码路径。上述结论依赖当前配置和部署边界，后续新增路由、代理配置或第三方集成时应重新复查。

## 2026-09-05 修复状态

- P1 请求体限制：现有 `decodeJSON` 已覆盖所有 JSON API，本轮补充了边界复查。
- P1 初始密码文件：已加入启动时文件校验、重启恢复关联，以及管理员首次改密后的自动删除。
- P1 CSP：静态 HTML 的内联脚本改为按实际内容生成 SHA-256 hash，默认脚本来源收紧为同源。
- P1 日志：新增密码文件清理失败时的统一脱敏日志，既有 API/远端错误脱敏保持有效。
- P2 捕获目录：已拒绝可写目录、符号链接、非普通文件和超大捕获文件。
- P2 SSE：已加入单用户和全局连接上限、心跳、写超时和会话失效检查。
- P2 依赖扫描：GitHub Actions 已加入 `govulncheck` 与 `pnpm audit` 步骤。

## 2026-09-08 继续修复状态

- 已新增全局 `WithRequestBodyLimit`，在路由分发前限制所有请求体为 64 KiB，并保留各接口更小的专用限制。
- 已补充分块请求、表单请求、SSE 路径、精确边界和尾随 JSON 内容测试。
- CSP 默认仅允许同源脚本；静态 HTML 内联脚本继续使用 SHA-256 hash。
- 日志脱敏扩展到 URL、绝对路径、控制字符和 Unicode 安全截断，并覆盖启动、调度器和管理日志。
- 仍建议后续补充真实反向代理链验收和 SSE 慢客户端压测；这两项需要部署环境支持。

本地 `go test ./...` 的网络型测试在受限沙箱中无法绑定 `127.0.0.1`/`::1` 临时端口；非网络包测试通过，代码编译与 `go vet` 可在允许本地监听的环境中复核。
