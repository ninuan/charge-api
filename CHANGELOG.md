# 更新日志

本文件记录面向使用者的重要变化。版本号遵循语义化版本规则。

## 1.5.1 - 2026-08-11

### 安全维护

- 登录和注册的外部人机验证由 Cloudflare Turnstile 替换为 hCaptcha，现有账户、会话和 SQLite 数据不受影响。
- 升级前需在服务器配置 `HCAPTCHA_REQUIRED`、`HCAPTCHA_SITE_KEY` 和 `HCAPTCHA_SECRET_KEY`；旧 `TURNSTILE_*` 配置不再生效。

### 新增

- 新增按整桩设置的空闲提醒：一个充电桩任一端口从忙碌变为空闲时生成提醒，不再要求逐端口配置。
- 新增站内通知中心和浏览器通知，可配置生效星期、生效时段、免打扰时段与浏览器权限。
- 新增受控后台调度：按桩号低频刷新、同桩请求复用、并发限制、每日额度、失败退避与凭据失效暂停。
- 管理后台新增提醒策略和计划断电窗口配置，并展示待执行桩数、24 小时成功率、缓存命中、合并请求和异常次数。

### 改进

- 默认在 Asia/Shanghai 时区的 23:00–07:00 计划断电期间暂停后台请求和离线提醒，恢复供电后随机分散请求。
- 连续调度异常会进入现有管理员异常列表；所有系统策略修改继续写入审计日志，并标明变更类别。
- 已解决通知按保留策略定期清理，未处理通知无论创建时间多久都不会被维护任务删除。
- 整桩空闲提醒规则、通知偏好、通知分页及管理员运维契约已纳入 1.5.1 OpenAPI 和前端类型漂移检查。

### 升级说明

- 首次启动会自动升级 SQLite 并补齐提醒默认策略；已有充电桩、历史、会话、异常和审计数据保持不变。
- 后台提醒默认启用，但只有用户明确为某个充电桩创建提醒规则后才会产生低频远端请求。
- 管理员可在“系统设置”中全局停用后台提醒，或按学校实际供电时间调整计划断电窗口。

## 1.5.0 - 2026-08-06

### 新增

- 新增设备与单端口历史：状态时间线、每日占用率、7×24 热力图、平均充电时长和繁忙/空闲提示。
- 新增管理员运营趋势，支持 24 小时、7 天、30 天范围以及 CSV 导出。
- 新增独立的端口历史保留设置和运维数据量展示，默认保留 90 天。
- `/healthz` 响应新增当前发布版本，服务启动日志也会记录版本号。

### 改进

- 端口状态只在首次观察或状态变化时记录，避免重复快照持续放大 SQLite。
- 指标使用聚合计数存储；管理员离线端口趋势改为一次扫描计算全部时间点。
- 趋势、热力图和端口变化使用轻量一次性动画，并完整支持 `prefers-reduced-motion`。
- OpenAPI 契约生成前端历史/趋势类型，CI 会检查契约与生成代码是否漂移。
- 备份门禁会实际恢复压缩文件，并执行 SQLite 完整性与数据一致性检查。

### 升级说明

- 首次启动会自动将 schema v7 升级到 v8，保留用户、会话、指标、异常和审计数据。
- 升级前仍建议执行一次 SQLite 在线备份；部署后可检查 `/healthz` 的 `version` 是否为 `1.5.0`。
- 历史记录从升级后首次获取到端口状态时开始，系统不会虚构升级前的状态。
