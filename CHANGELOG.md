# 更新日志

本文件记录面向使用者的重要变化。版本号遵循语义化版本规则。

## 1.5.4 - 2026-09-16

- 主页快照加载不再等待提醒和通知服务，辅助功能独立显示加载与错误。
- 新增公告管理、有效期和定时发布、账号确认状态及重新提醒。
- 用户主页显示轻量摘要，支持详情、回看与重要公告持续展示。
- 退出登录并入账户资料行；统一用户端、管理端普通操作按钮尺寸，手机端刷新与添加操作同排等宽。
- 修复通知授权按钮对齐、微信操作与设置提交布局，以及窄屏创建邀请码弹窗溢出。
- 修复登录和注册页密码显示按钮按下时跳出输入框。
- 消除开发模式重复初始化请求，优先加载主快照；修复后台跨过公告有效期边界后返回页面时更新延迟。
- 修复公告提交缺少 JSON 请求头导致创建、发布、编辑及确认失败；避免复制公告的旧响应在切换页面后触发跳转。

## 1.5.3 - 2026-09-13

### 改进

- 添加充电桩前检查平台绑定，未绑定时在同一弹窗内扫码；绑定后返回原表单，保留输入且不自动提交。
- 微信授权成功后才启用“确认绑定”，持续显示明确的最后一步提示；区分绑定已保存与平台凭据同步失败。
- 添加失败需要绑定或重新扫码时，提供就地恢复入口；未配置扫码服务时保留手动 Cookie 设置。
- 串行检测扫码状态，处理断网、二维码失效、连续失败和旧请求；确认响应丢失后按同一会话核对结果。
- 后端增加二维码会话归属、授权校验和短期幂等结果；长轮询使用独立超时，不改变普通接口等待时间。
- 确认、同步和解绑按账户协调，避免并发请求或旧凭据恢复覆盖新绑定；新绑定保存后旧二维码失效。

### 升级

- 前后端一起更新；不涉及数据库迁移。升级或服务重启后，尚未完成的二维码需重新生成，已有绑定与充电桩保留。
- 本版不承诺解决长期凭据失效问题；真实微信扫码仍需在部署环境人工复测。

## 1.5.2 - 2026-08-24

### 新增

- 空闲提醒默认改为 1、2、4 小时或持续到计划断电前的临时任务；创建时先检查一次，首次发现空闲、到期或取消后自动停止。
- 停用固定星期和时段提醒；升级时保留既有长期规则配置但关闭执行，界面只开放临时提醒。
- 通知中心新增 WxPusher 参数二维码绑定、总开关、四类消息偏好、测试消息、最近投递状态和解除绑定。
- 管理员运维页新增活动临时/固定提醒、过去 24 小时实际桩号请求数、WxPusher 队列、受理率、处理成功率和连续失败状态。

### 可靠性与安全

- SQLite Schema v12 增加提醒生命周期、AES-256-GCM 加密的 WxPusher UID、短期扫码会话和持久投递队列，并安全停用既有长期规则。
- 站内通知与外部投递任务在同一事务中创建；WxPusher 异常不会阻塞充电桩刷新、状态历史、SSE 或站内通知。
- 可重试错误最多重试三次；供应商已经受理的任务只查询结果，不重复发送；服务重启后继续处理未完成任务。
- 免打扰时段不发送浏览器或微信即时提醒，也不会在结束后补发过期消息；站内通知始终保留。
- AppToken 仅从服务端环境变量读取，完整 UID 加密保存且不会进入前端、管理员响应或日志。
- 通知保留任务会清理过期的信息类通知和已解决问题，同时继续保留尚待处理的离线与凭证问题。

### 改进

- 明确区分“等待发送”“WxPusher 已受理”“WxPusher 已处理”和客户端是否实际展示，失败状态提供可执行的恢复建议。
- 无活动临时任务或固定提醒时不产生后台请求；默认十分钟间隔、23:00–07:00 断电窗口下，单个持续任务每天最多发起 96 次桩号请求。
- 桌面端和 375px 移动端均可完成临时提醒、固定提醒、微信绑定、设置、测试和解绑。

### 升级说明

- 首次启动会把旧数据库升级至 Schema v12；原有站内通知、设备、凭据和历史数据保持不变，既有长期规则会保留配置但停止执行。
- 微信提醒是可选能力；未配置 `WXPUSHER_APP_TOKEN` 时，站内通知和浏览器提醒继续正常工作。
- 启用微信提醒时同时配置 HTTPS `PUBLIC_BASE_URL`，升级后可按 [`RELEASE_NOTES.md`](RELEASE_NOTES.md) 完成验证与回滚准备。

## 1.5.1 - 2026-08-11

### 安全维护

- 登录连续失败后启用内置图片验证码，注册继续使用图片验证码，不再依赖境外人机验证服务。
- 图片验证码改为一次性 PNG，并与登录限流及失败锁定协同工作；现有账户、会话和 SQLite 数据不受影响。

### 新增

- 新增按整桩设置的空闲提醒：一个充电桩任一端口从忙碌变为空闲时生成提醒，不再要求逐端口配置。
- 新增站内通知中心和浏览器通知，可配置生效星期、生效时段、免打扰时段与浏览器权限。
- 新增受控后台调度：按桩号低频刷新、同桩请求复用、并发限制、每日额度、失败退避与凭据失效暂停。
- 管理后台新增提醒策略和计划断电窗口配置，并展示待执行桩数、24 小时成功率、缓存命中、合并请求和异常次数。

### 改进

- 默认在 Asia/Shanghai 时区的 23:00–07:00 计划断电期间暂停后台请求和离线提醒，恢复供电后随机分散请求。
- 连续调度异常会进入现有管理员异常列表；所有系统策略修改继续写入审计日志，并标明变更类别。
- 已解决通知按保留策略定期清理，未处理通知无论创建时间多久都不会被维护任务删除。
- 整桩空闲提醒规则、通知偏好、通知分页及管理员运维契约已纳入 1.5.1 OpenAPI 和前端类型漂移检查。

### 升级说明

- 首次启动会自动升级 SQLite 并补齐提醒默认策略；已有充电桩、历史、会话、异常和审计数据保持不变。
- 后台提醒默认启用，但只有用户明确为某个充电桩创建提醒规则后才会产生低频远端请求。
- 管理员可在“系统设置”中全局停用后台提醒，或按学校实际供电时间调整计划断电窗口。

## 1.5.0 - 2026-08-06

### 新增

- 新增设备与单端口历史：状态时间线、每日占用率、7×24 热力图、平均充电时长和繁忙/空闲提示。
- 新增管理员运营趋势，支持 24 小时、7 天、30 天范围以及 CSV 导出。
- 新增独立的端口历史保留设置和运维数据量展示，默认保留 90 天。
- `/healthz` 响应新增当前发布版本，服务启动日志也会记录版本号。

### 改进

- 端口状态只在首次观察或状态变化时记录，避免重复快照持续放大 SQLite。
- 指标使用聚合计数存储；管理员离线端口趋势改为一次扫描计算全部时间点。
- 趋势、热力图和端口变化使用轻量一次性动画，并完整支持 `prefers-reduced-motion`。
- OpenAPI 契约生成前端历史/趋势类型，CI 会检查契约与生成代码是否漂移。
- 备份门禁会实际恢复压缩文件，并执行 SQLite 完整性与数据一致性检查。

### 升级说明

- 首次启动会自动将 schema v7 升级到 v8，保留用户、会话、指标、异常和审计数据。
- 升级前仍建议执行一次 SQLite 在线备份；部署后可检查 `/healthz` 的 `version` 是否为 `1.5.0`。
- 历史记录从升级后首次获取到端口状态时开始，系统不会虚构升级前的状态。
