import Link from "next/link"
import type { ReactNode } from "react"

export function AuthPageShell({ mode, children }: { mode: "login" | "register"; children: ReactNode }) {
  const register = mode === "register"
  return <main className="min-h-dvh bg-muted/35"><header className="border-b bg-background"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"><Link href="/login" className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-lg bg-primary text-sm font-bold text-primary-foreground">C</span><span><strong className="block text-sm tracking-tight">Charge Console</strong><small className="block text-xs text-muted-foreground">充电设施运营中心</small></span></Link><p className="text-sm text-muted-foreground">{register ? "已有账户？" : "还没有账户？"} <Link className="font-medium text-foreground underline underline-offset-4" href={register ? "/login" : "/register"}>{register ? "去登录" : "注册账户"}</Link></p></div></header><div className="mx-auto grid max-w-7xl gap-8 px-4 py-10 sm:px-6 lg:grid-cols-[minmax(0,1fr)_28rem] lg:items-center lg:px-8 lg:py-20"><section className="hidden max-w-xl lg:block"><p className="text-xs font-medium tracking-widest text-muted-foreground uppercase">Infrastructure, clearly seen</p><h1 className="mt-4 text-5xl font-semibold tracking-tight">十个端口，<br />每个状态都清楚。</h1><p className="mt-6 max-w-lg text-base leading-7 text-muted-foreground">按需刷新远端数据，独立管理个人凭据，在一个安静、可靠的工作台里掌握设备占用和剩余时间。</p><dl className="mt-12 grid grid-cols-3 gap-6 border-t pt-6 text-sm"><div><dt className="font-medium">主动刷新</dt><dd className="mt-1 text-xs leading-5 text-muted-foreground">避免无意义请求</dd></div><div><dt className="font-medium">凭据隔离</dt><dd className="mt-1 text-xs leading-5 text-muted-foreground">每个用户独立</dd></div><div><dt className="font-medium">退避保护</dt><dd className="mt-1 text-xs leading-5 text-muted-foreground">请求节奏可控</dd></div></dl></section><section className="w-full rounded-xl border bg-background p-6 shadow-sm sm:p-8"><p className="text-xs font-medium tracking-widest text-muted-foreground uppercase">{register ? "Create account" : "Welcome back"}</p><h2 className="mt-2 text-2xl font-semibold tracking-tight">{register ? "注册普通用户" : "登录运营工作台"}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{register ? "创建账户后，即可配置个人 Cookie 和充电桩。" : "管理员进入监控中心，普通用户进入个人设备看板。"}</p><div className="mt-7 grid grid-cols-2 rounded-lg bg-muted p-1"><Link className={`rounded-md px-3 py-2 text-center text-sm font-medium ${!register ? "bg-background shadow-sm" : "text-muted-foreground"}`} href="/login">登录</Link><Link className={`rounded-md px-3 py-2 text-center text-sm font-medium ${register ? "bg-background shadow-sm" : "text-muted-foreground"}`} href="/register">注册</Link></div><div className="mt-7">{children}</div><p className="mt-7 text-center text-xs leading-5 text-muted-foreground">登录状态最长保留 7 天。公共设备上使用后请及时退出。</p></section></div></main>
}
